.DS_Store
.venv/
node_modules
dist
*.log

# Large local datasets (keep out of Git unless using LFS)
public/data/*.zarr/
public/data/*.nc

# If you *do* want to publish the Greenland Sea Zarr store via GitHub Pages,
# uncomment the following two lines (or remove the ignore above):
!public/data/GS.zarr/
!public/data/GS.zarr/**
